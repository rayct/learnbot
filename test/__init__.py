# Initialization code for the 'tests' package

# Importing the modules to make them available when the package is imported
# There's no need to import anything here since test modules are not typically imported
# However, you can add imports if necessary for test setup or utilities

# Setting up package-level variables or constants
TEST_VERSION = "1.0.0"
